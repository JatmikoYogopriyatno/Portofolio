<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Portofolio" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/biru_tinta</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">@color/kuningan</item>
        <item name="android:colorBackground">@color/kertas</item>
        <item name="colorSurface">@color/kertas</item>
        <item name="colorOnSurface">@color/tinta</item>

        <!-- Bilah status menyatu dengan warna halaman, bukan hitam polos. -->
        <item name="android:statusBarColor">@color/biru_tinta</item>
        <item name="android:navigationBarColor">@color/kertas</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>
</resources>
